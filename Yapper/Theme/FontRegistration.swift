// Yapper uses system fonts and does not ship or register custom font files.
enum AppFonts {
    // Retained for the existing launch call; no font registration is necessary.
    static func registerBundledFonts() {}
}
