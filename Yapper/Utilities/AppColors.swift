import SwiftUI

extension Color {
    // Shared aliases used by older screens.
    static let appRed = Color.accentPrimary
    static let sidebarBackground = Color.bgSidebar
    static let contentBackground = Color.bgContent
}
